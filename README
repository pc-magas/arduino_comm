Arduino communication library
Ths library allows you to communitate with arduino through USB Serial Interface.

Compile in terminal:
	gcc -o <exevutalble> <your project's c files> ArduinoIO.c

In your code intergrade:

	#include "ArduinoIO.h"

on the first line
