
Arduino communication library
Ths library allows you to communitate with arduino through USB Serial Interface.

Compile in terminal:
	gcc -o <executable> <code depends to> Arduino.c
In your code include the line for the preprocessor:
	#include "Arduino.h"
